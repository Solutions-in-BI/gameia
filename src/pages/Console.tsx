/**
 * Console - Página de Configuração da Plataforma
 * Área exclusiva para admins da empresa
 */

import { ConsoleCenter } from "@/components/console/ConsoleCenter";

const Console = () => {
  return <ConsoleCenter />;
};

export default Console;
